# Sweep max_bars for mid loss limiter (keep min_bars=13, stop_ret=-0.010)
kind: grid
base: configs/trend_based/current.yaml
mode: three_block
initial_capital: 1000.0
dd_cap_percent: 20
top_k: 12

grid:
  mid_loss_limiter.max_bars: [24, 32, 40, 48, 56, 64]
