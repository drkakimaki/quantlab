# Small grid over exit knobs (keep constrained to reduce overfit risk)
kind: grid
mode: three_block
base: configs/trend_based/current.yaml
dd_cap_percent: 20
initial_capital: 1000
top_k: 30

grid:
  pipeline.6.params.stop_ret: [-0.008, -0.01, -0.012]
  pipeline.7.params.bar_n: [18, 24, 30]
  pipeline.7.params.min_ret: [-0.0025, -0.005, -0.0075]
