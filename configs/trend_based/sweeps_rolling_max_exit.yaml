# Sweep rolling-max exit gate (stagnation filter)
kind: grid
base: configs/trend_based/current.yaml
mode: three_block
initial_capital: 1000.0
dd_cap_percent: 20
top_k: 12

grid:
  rolling_max_exit.window_bars: [12, 18, 24, 36]
  rolling_max_exit.min_bars: [12, 18, 24]
  rolling_max_exit.min_peak_ret: [-0.010, -0.005, -0.002, 0.000]
